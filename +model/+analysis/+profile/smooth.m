function out = smooth(Profile,FilterWidth)
% model.analysis.profile.smooth  Smooths data in Profile with moving average filter of width FilterWidth

    out = smooth(Profile,FilterWidth); % smooth with moving average filter

end